services:
  - type: web
    name: ishiva-digital-matrix
    env: docker
    region: oregon
    plan: free
    dockerCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 180 --workers 2 --threads 4
    envVars:
      - key: FLASK_SECRET_KEY
        generateValue: true
